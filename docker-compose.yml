services:
  portfolio:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ADMIN_PASSWORD=changeme
      - NODE_ENV=production
      - ORIGIN=http://localhost:3000
    volumes:
      - ./content:/app/content
    restart: unless-stopped
