services:
  portfolio:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - ORIGIN=http://localhost:3000
    volumes:
      - ./content:/app/content
    restart: unless-stopped
